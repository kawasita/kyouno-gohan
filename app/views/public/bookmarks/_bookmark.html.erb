<% if recipe.bookmarked_by?(current_user) %>
  <%= link_to "お気に入り解除", recipe_bookmarks_path(recipe), method: :delete, remote: true, class: 'btn btn-danger' %></td>
<% else %>
  <%= link_to "お気に入り登録", recipe_bookmarks_path(recipe), method: :post, remote: true, class: 'btn btn-info' %></td>
<% end %>
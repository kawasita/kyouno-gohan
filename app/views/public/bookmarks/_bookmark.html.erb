<% if recipe.bookmarked_by?(current_user) %>
  <%= link_to "ブックマークを外す", recipe_bookmarks_path(recipe), method: :delete, remote: true %></td>
<% else %>
  <%= link_to "ブックマーク", recipe_bookmarks_path(recipe), method: :post, remote: true %></td>
<% end %>
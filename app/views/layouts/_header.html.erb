<header class="sticky-top">
  <nav class="navbar navbar-expand-lg" style="background-color:#c49a6a">
    <div class="container">
      <a class="navbar-brand" href="/"><span class="font-weight-bold"><i class="fa-thin fa-plate-utensils"></i>ながのCAKE</span></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation" style="background-color:#deb887;">
        <span class="navbar-toggler-icon"></span>
      </button>

        <div class="navbar">

          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li><%= link_to user_path(current_user.id),class: 'btn btn-outline-light' do %> マイページ <% end %></li>
              <li><%= link_to destroy_user_session_path, method: :delete,class: 'btn btn-outline-light' do %> ログアウト <% end %></li>
            <% else %>
              <li><%= link_to about_path,class: 'btn btn-outline-light' do %> About <% end %></li>
              <li><%= link_to new_user_registration_path,class: 'btn btn-outline-light' do %> 新規登録 <% end %></li>
              <li><%= link_to new_user_session_path ,class: 'btn btn-outline-light' do %> ログイン <% end %></li>
            <% end %>
            </ul>
          </div>

          <div class="container-fluid">
            <a class="navbar-brand"></a>
            <form class="d-flex">
              <input class="form-control me-2" type="search" placeholder="今日のごはん" aria-label="Search">
              <button class="btn btn-outline-light" type="submit"><i class="fas fa-search"></i></button>
            </form>
          </div>

      </div>

    </div>
  </nav>
</header>
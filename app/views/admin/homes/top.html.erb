<div class="container">
  <div class="row">
    <div class="col-12">
      <div class="col-6">
        <h3 class="p-3">新着コメント一覧</h3>
      </div>
      <% if @comments.blank? %>
        新着コメントはありません
      <% else %> 
        <%= render "admin/homes/search-form" %>
        <div>
          <%= render 'admin/homes/comment_index', comments: @comments %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="justify-content-center text-center">
    <%= paginate @comments %>
  </div>
</div>